export type CategoryType = {
    categoryName: string;
    _id: string;
  };

export  type FoodType = {
      name: string;
      category: string;
      price: string;
      image: string;
      ingredients: string;
      _id: string
  };

