export type CategoryType = {
    categoryName: string;
    _id: string;
  };

export  type FoodType = {
      name: string;
      category: string;
      price: number;
      image: string;
      ingredients: string;
      _id: string
  };

